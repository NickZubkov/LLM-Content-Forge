namespace ContentForge
{
    /// <summary>Item rarity tiers. Order matters — must match the server's allowed values.</summary>
    public enum Rarity
    {
        Common,
        Uncommon,
        Rare,
        Epic,
        Legendary
    }
}
